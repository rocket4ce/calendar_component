// Main entry point that bundles both JavaScript and CSS
import CalendarHooks from "./calendar-hooks.js";
import "@event-calendar/core/index.css";

// Re-export the hooks as default export
export default CalendarHooks;
